# Session 138 — THE REAL DOGFOOD: `vajra claude` run from INSIDE chitra

> **Status: APPROVED** — founder, in chat at the S138 start: *"start session session 138, all
> approved"*, then chose **park session-16 + lock a fresh chart** and **founder runs `vajra claude`
> interactively inside chitra** (I prep + record). Recorded here as the approval token this prompt
> required.
>
> Founder directive in force (S118): README/VISION claims are the target spec, never softened.

## Why this session exists (the S137 correction)

S137 was *meant* to be the first dogfood of Vajra governing a real BUILD in an outside project. It was
run the **wrong way**: the session ran inside the Vajra repo and reached into chitra from the outside
with plain `git`/file commands — a Vajra chat poking chitra across the fence. chitra's own hooks never
fired, the dispatched fleet was Vajra's, and the "cross-repo Coder-gate blind spot" was reported as a
product finding when it was really an **artifact of the wrong setup**. Vajra did not fail; the method
was wrong, and no real user would ever run it that way.

**S138 fixes the method.** The build is a **native chitra session**: `cd chitra && vajra claude`, run
interactively by the founder. chitra's hooks fire, chitra's fleet is dispatched, chitra's `.ai/` is the
governance, chitra's session number and branch. The real question this session answers:
**does Vajra actually work as the resident manager of a repo that isn't its own?**

## Type

**CODE — a PAID dogfood, founder-driven.** The real code lands in chitra. **This Vajra session is the
wrapper**: it preps the run (read-only), hands the founder a copy-paste brief, and — after the run —
records the evidence, writes the verify script, and reports what Vajra's governance actually did.
No `src/` change is expected in the Vajra repo. Max 2 assumptions · 2 retries · ~2h · 1 story · new
chat · approval token before any commit.

## The one story

Lock chitra's **`heatmap`** chart to its reference/panel design language (the family scatter joined at
chitra's session 17), decided **with the founder**, via a `vajra claude` session run **from inside
chitra**. `heatmap` is the next unlocked chart in chitra's catalog order (`CHARTS[]` in
`artifacts/chitra-docs/src/data/charts.ts`, position 9), after the locked-and-merged set
(line · bar · area · scatter · pie · donut) and skipping the parked in-flight session-16 charts
(sparkline · histogram). **The founder may redirect to another unlocked chart at the run start.**

## Two repos, two governances — keep them straight

- **chitra is where the code lands, and where the governance runs.** The heatmap lock is a native
  chitra session (chitra decides its own next session number + branch off chitra `main`), with real
  commits under **chitra's OWN constitution, hooks, and fleet** (`chitra/.ai/`, `chitra/CLAUDE.md`,
  `chitra/.claude/`). Obey chitra's rules inside chitra. Do NOT import Vajra's.
- **This Vajra session is the dogfood wrapper only.** Its deliverables are the prep, the handoff
  brief, the evidence, the reports, and the receipt. **It does NOT edit chitra, does NOT commit in
  chitra, and does NOT dispatch chitra's build fleet** — the S137 mistake, never repeated. If a
  Vajra `src/` change becomes necessary, that is a finding to record, not a silent expansion.

## The reference design language `heatmap` must adopt (chitra's locked contract, adapted)

The family contract (verbatim from chitra's locked scatter/bar/line blocks) — the design-advisor
inside chitra proposes how each rule maps onto a **grid/matrix** geometry; the founder locks it on the
render:

- **One accent hue, spent once**, on the single most salient cell (e.g. the max-value cell) — never a
  `theme.colors[i % n]` rainbow. Everything else uses the documented grey tone ramp
  `#ECECEF → #C6C6CE → #A4A4AE → #6A6A75` (a heatmap's intensity ramp IS the grey ramp; the accent
  marks the peak).
- **Panel language**: dashed frame (`┌╌…╌┐`), eyebrow row (uppercase, letter-spaced), the `│`/`+`
  guide language, and the `│ ╌…╌ │` rule separators, exactly as the locked charts use them.
- **Summary footer** reporting facts true for arbitrary matrix data (e.g. `rows·cols · min..max ·
  peak (r,c)`), with the peak in the accent — decided WITH the founder.
- **Empty / degenerate data is safe** (empty grid → framed panel, `n 0`-style footer, no
  `Infinity`/`NaN`).
- Docs/previews regenerated by chitra's own generator (`pnpm gen:charts` — previews are DERIVED,
  never hand-edited); chitra's README gains a `### LOCKED: heatmap chart — session NN design` block.

## Deliverables

- **`heatmap()` locked in chitra**, real commits on a native chitra session branch, chitra's own
  verify/pipeline green, the README `LOCKED: heatmap` contract added, previews regenerated — all done
  BY the chitra `vajra claude` session, not by this Vajra chat.
- **The founder's design verdict** — heatmap rendered and LOOKED AT (not merely diffed), signed off
  by the founder as matching the locked language (the S134 "seen, not read" bar).
- **The resident-manager report** (`sessions/session-138-summary.md` + `sessions/session-138-review.md`):
  the honest answer to *did Vajra work as chitra's resident manager, run the way a real user runs it?*
  — which of chitra's hooks fired, which of chitra's fleet roles were dispatched (and whether the
  advice CHANGED the work), which gates bound, the **authoritative `$`** (honest null for an
  interactive run — S77 — recorded with the reason) and the **RAW subagent token total** (never
  new-tokens-only — the S134 45× / S135 20× trap).
- **chitra proved undisturbed the S134 four ways** outside the intended paths (HEAD / index hash /
  stash list / branch), with the parked session-16 WIP restorable byte-identical, every new path
  pre-declared by name.
- `scripts/verify-session-138.sh` (exits 0, machine-local checks FAIL-on-absent, never skip — S69) +
  `sessions/session-138-summary.md` with exactly 3 ranked next candidates.

## Acceptance (testable, EARS-style)

1. WHEN chitra's `heatmap()` is rendered after the lock, THEN its output carries the panel language
   (dashed frame · eyebrow · one-accent-spent-once + grey ramp · guides · summary footer) — asserted
   by chitra's OWN tests/generator, verified at the raw-glyph/RGB level that the accent hue appears
   exactly once and the ramp is the documented grey ramp (the S134 method).
2. WHEN chitra's own verify/pipeline runs on the heatmap branch, THEN it is green, and the README
   carries a real `### LOCKED: heatmap chart` block (a hand-edited preview or a missing contract FAILS).
3. The founder SIGNS OFF on the rendered heatmap as matching the locked language (a recorded verdict,
   the human's call — this session proposes, the founder locks).
4. The dogfood was run **the corrected way**: `vajra claude` from INSIDE chitra, interactively, so
   chitra's own hooks fired and chitra's own fleet was dispatched. The report NAMES what governance
   actually did (hooks · roles · gates), and records the authoritative `$` (or the honest null + reason)
   AND the RAW subagent token total from the on-disk transcripts.
5. chitra's parked session-16 WIP and every chart already locked are UNDISTURBED (proven the four
   ways); exactly the heatmap files + regenerated previews changed, each pre-declared; the session-16
   stash restores byte-identical (recorded tree sha).

## Design (the Architect gate)

- design-significant: yes — the significance lives in **chitra**: this authors a NEW locked contract
  (`### LOCKED: heatmap chart` in chitra's README) and deviates from the current `heatmap.ts`
  (any rainbow/ad-hoc intensity → accent-once-on-peak + documented grey ramp; bare grid → dashed
  panel + eyebrow + guides + footer). On the Vajra side it is a dogfood with no new interface.
- **design-advisor: skipped — the Vajra wrapper introduces NO design-significant Vajra change (no new
  Vajra interface); the design significance lived entirely in chitra, and chitra's OWN design-advisor
  should have proposed the accent-cell rule + footer under its Architect mandate. It did NOT run —
  only chitra's tech-lead + fidelity-reviewer were dispatched inside the build. That gap is this
  session's fakest green, disclosed in `sessions/session-138-review.md` (rec 2) and the summary, not
  silenced. The heatmap design was authored by the headless build agent and locked by the founder's
  seen-not-read sign-off; correctness was verified independently at raw-RGB (verify #9), but it was
  NOT governed by the design role. The next session (candidate A) runs the interactive path where this
  mandate can be observed live.**
- design record cited: this prompt + chitra's README locked blocks (line/bar/area/scatter, all present
  on chitra `main`). Vajra-side there is no new ADR — the dogfood introduces no Vajra interface.

## Plan (ordered — cite the acceptance criteria each step covers)

1. (Vajra, read-only) Confirm the next unlocked chart (heatmap) + the reference contract; write the
   founder handoff (`sessions/session-138-chitra-handoff.md`): the safe park command, the exact
   interactive-run commands, the design brief to paste, and the file-overlap warning. covers: 5
2. (chitra, founder-driven) Park session-16 WIP; run `vajra claude` inside chitra; the native session
   dispatches chitra's crew, locks `heatmap()` to the language, regenerates previews, adds the README
   contract, and lands real commits under chitra's rules. covers: 1, 2, 4
3. (chitra, founder) Render heatmap and sign off (seen, not read). covers: 3
4. (Vajra) Record the resident-manager evidence (hooks/roles/gates, authoritative `$` or honest null,
   RAW subagent tokens); prove chitra undisturbed four ways + session-16 restored byte-identical;
   write `verify-session-138.sh` + the two reports. covers: 4, 5

## Execution (the Coder gate — record each plan step's landing commit as work lands)

**Cross-repo note (the corrected method):** step 2's build commits are **chitra** commits made by a
**native chitra session**, so they resolve inside chitra — NOT a cross-repo reach. Vajra's Coder gate
lives only in `vajra next --advance` (not in `verify-closeout.sh`), and this dogfood does not run
`--advance` to close, so the Vajra ship gate is unaffected. Vajra-side evidence steps cite real Vajra
shas; chitra build steps cite real chitra shas, annotated as chitra.

- step 1 — done: d7c1cae (Vajra: prompt + handoff). covers: 5
- step 2 — done: e538751, 1ad9c6c, 22b34f9, e2b6bb9 (chitra, native `session-18-heatmap-lock`). covers: 1, 2, 4
- step 3 — done: render `sessions/session-138-heatmap-render.html`; founder signed off in chat ("looks good and impressive") — no code commit. covers: 3
- step 4 — done: eee1db1 (Vajra: verify + demo + render) and the closeout bundle that follows (reports + receipt + undisturbed proof + `.ai/` sync). covers: 4, 5

## Guardrails

- **THE CORRECTION IS THE POINT: never reach across the fence.** This Vajra chat does read-only prep
  and recording. It does NOT edit chitra, commit in chitra, or dispatch chitra's build fleet. The
  build is a native `vajra claude` session the founder runs inside chitra. (feedback-run-vajra-inside-target-repo)
- **Never characterize a setup/harness artifact as a product discovery** (the S137 lesson). Before
  claiming "Vajra can't do Y", ask: was Vajra run the way a user runs it? Report the setup honestly first.
- **chitra has its own constitution, hooks, and committed ten-role fleet (S136) — obey chitra's rules
  inside chitra. Do NOT disturb the parked session-16 WIP** (restore it byte-identical).
- **The design is decided WITH the founder** — this session proposes; the founder looks at the
  rendered chart and signs off. "Seen, not read" (S134).
- **Budget every subagent dispatch inside chitra TIGHT: named files, never "read the repo"** (S134) —
  the founder is on a $20/month plan; S134's three broad dispatches hit the limit.
- **Report the RAW subagent token total, never a new-tokens-only figure** (S134 45× / S135 20× catch).
- **If a dispatch dies mid-flight on a spend limit, record the result as INCOMPLETE** (S134).
- **Watch for the S135 bootstrapping wall / stale-agent snapshot** — chitra's committed agent files
  are dispatchable in a FRESH chitra chat; if a role is "not found", that is the wall, not a missing file.
- **What must NOT be claimed:** ten role FILES present is not ten roles USED; chitra read `0 of 8`
  stations at S134. Report what the crew actually DID, not what the roster makes possible.
- Un-forgeable commit marker on every Vajra-side commit; obey chitra's commit rules on the chitra side.
- Attest LAST (S69/S131): recompute `--inputs-sha 138` after every edit to this prompt; run the full
  `verify-closeout.sh` on the branch BEFORE merging (S83). **Next GT: S140.**

## Delta (vs ROADMAP — OpenSpec markers)

- `+` the FIRST dogfood run **the way a real user runs it** — `vajra claude` interactively from INSIDE
  chitra, chitra's own hooks/fleet/`.ai/` governing a real BUILD (S137's corrected method).
- `+` chitra's `heatmap()` joins the locked reference language (7 of 20 charts locked+merged).
- `~` the dogfood evidence extends from "Vajra reaching across into chitra" (S137, wrong) to "Vajra
  resident inside chitra" (S138, correct) — the honest answer to whether Vajra works for a user.
- `-` "make the Coder gate repo-aware" retired (it was only needed because of the wrong setup).
