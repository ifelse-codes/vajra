# Session 51 — Measure the value gap (real-task baseline, PAID) · direction B, CODE/VERIFY

> **Founder pick at S50 GT close (A):** the standing #1 is **work-quality is UNMEASURED**. S48 obedience +
> S49 baseline measure only obedience to the RAILS (a floor). This session takes the first real reading of
> the thing direction B actually promises — **does the AI do BETTER WORK through Vajra?** — and, as a paid
> run, **refreshes the aging `dogfood_check`** (last live fire = S46) in the same spend.

## Why this session
- S50 GT verdict: paper moat intact, **live moat aging** — no paid `vajra claude` run since S46 (S47/S48/S49
  all ~$0). `dogfood_check` = **🟡 measured-then-aging.**
- The work-quality thesis of direction B has **never been measured by a number.** Obedience is a floor, not
  proof the output was better. This is the harder, truer measurement (the S49-B carry).
- A paid run does **double-duty**: (1) the value-gap reading; (2) a live moat fire as a side effect.

## The task (one small, fair real task — pick before starting)
Run the **same** small real coding task twice:
- **Arm A — through Vajra:** `vajra claude` (co-pilot murmur + loader + receipt live).
- **Arm B — plain:** `claude` with no Vajra context.
Diff the two on a **pre-declared rubric** (declare it before running, so the diff isn't post-hoc rationalized):
1. **Correctness** — did the output actually work? (tests/build/manual check)
2. **Corrections** — how many founder interventions / re-work rounds to get it right?
3. **Cost** — $ per arm from the receipt/ledger.
Task must be genuinely fair + small (fits the ~2h + 1-story cap). Single sample — say so honestly.

## What counts as done (success criteria)
- A written baseline: `sessions/session-51-summary.md` with the rubric, both arms' raw results, and an
  **honest verdict** — did Vajra measurably improve correctness / reduce corrections, or not (with the caveat
  that n=1)?
- The `dogfood_check` refresh recorded: cost-ledger line for the paid run + whether the moat fired live.
- Exactly 3 ranked next candidates for S52 (title · one-sentence goal · why-pick · key risk), from ROADMAP.

## Guardrails
- **PAID** — budget cap `$5.00` warn-mode; keep the task small. Log the real spend to the cost ledger.
- Max 2 assumptions · max 2 retries · ≤3 files/commit · ~2h cap · 1 story.
- Branch `session-51-<slug>` off `main` — **new chat.** Approval token before any commit.
- Darshan for every human reply · Varta against the live `.ai/`.
- **Do NOT re-open the enforcement arc** — the moat is the floor; this session measures work-quality (the S46
  pivot: stop polishing the guard, prove the value).

## Honest-read reminder (carry from S48/S49)
- Obedience% + baseline = the **floor** (obedience to RAILS), not work-quality. This session is the first
  attempt at the work-quality number itself. n=1 is a start, not a proof — frame it that way.

## Output
- `sessions/session-51-summary.md` (the value-gap baseline + verdict + 3 S52 candidates).
