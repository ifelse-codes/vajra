# Current Task Pointer

**Thin pointer. Real session briefs live under `prompts/`.**

## Session 35 — Ground Truth (NO-CODE) — COMPLETE

- **Type:** GROUND TRUTH, mandated (`35 % 5 == 0`). Lens A (founder pick): "fix the core before breadth" bet verification + second-agent gate re-measure.
- **All 8 required audits run.** **Gate call: NOT cleared — still unmeasured.** Zero `vajra claude` spend since S31; S32–S34 fixes are test-verified, not daily-use-verified. Same honest call as S30.
- **Tension pressure-test:** `.claude/settings.json` merge gap + `exit_code` heuristic gap are 2 isolated debt items, not a structural wedge leak.
- **Ranked S36 candidates:** (1) real dogfood session (recommended), (2) `.claude/settings.json` merge, (3) `exit_code` heuristic fix, (4) obedience metric (backlog).
- **Report:** `sessions/session-35-ground-truth.md`. No code, no commits to source, no PR (GT rule) — `.ai/` sync only.

Between sessions. Next: founder picks option A/B/C from the report.

## Next Session

Founder picks from 3 options in `sessions/session-35-ground-truth.md`:
- **A (recommended, prompt pre-written):** `prompts/36-task-real-dogfood-run.md` — run
  `vajra claude` on real work, un-block the gate.
- **B:** `.claude/settings.json` merge on init (no prompt yet — write one if picked).
- **C:** `exit_code == Some(0)` heuristic fix for `cargo`/`npm`/`pytest` (no prompt yet).

## Build Queue (from ROADMAP.md, in order)

1. ~~Darshan enforcement (S32)~~ — **DONE.**
2. ~~Compression schema fix (S33)~~ — **DONE.**
3. ~~Brownfield onboarding (S34)~~ — **DONE.**
4. ~~S35 = GROUND TRUTH (NO-CODE)~~ — **DONE.** Gate call: unmeasured, not cleared.
5. **S36 = founder pick (A recommended: real dogfood run)** · `.claude/settings.json` merge · `exit_code` heuristic fix · obedience metric (backlog) · second agent (gate still unmeasured).

## Always-True Reminders

- Load order: `.ai/AGENTS.md` + `.ai/CONSTRAINTS.yaml#load_order`.
- Branch: `session-NN-<slug>`.
- Every 5th session is NO-CODE ground-truth (last = S35; **next = S40**) — audits **direction + discipline** drift.
- Approval tokens: `approved`, `lgtm`, `ship it`, `yes commit`, `go ahead and commit`, `go ahead`.
- **New session = new chat** (enforced by the session-guard hook, in `vajra init` too).
- **Every fix moves a feature from *advised* → *enforced*** (S31 meta-finding — Vajra's own wedge; held 3×, no 4th data point yet).
