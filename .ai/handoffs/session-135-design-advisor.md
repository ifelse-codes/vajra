---
role: design-advisor
session: 135
agent: claude-code-subagent (verified: toolu_01NwgXhUNehXYXNsWQnVVPLv)
source-sha: 27b9939af3afa91b37924f22ea0e5a2e4ad94d285cae468d64e4ac432bfd365d
captured: 2026-08-27T12:28:36Z
cost_usd: null
---

# Design-advisor handoff — session 135

## Design Advisor brief — S135 `tech-lead`

I read the whole prompt, `src/mandate/mod.rs` lines 1–72 plus the ladder/`mandate_gate` signature, and confirmed `DECISION-007-agent-fleet.md` carries both the `S133 addendum` (line 862) and the `S134 addendum` (line 947). All citations below resolve to real files.

The prompt's own `## Design` block is already well-formed (real rationale, no placeholders, marker set to `yes`, both citations exist). My job is to confirm the marker/citation and to close the three OPEN questions with rejected alternatives. The builder's leanings are largely right; I concur with two and sharpen the third.

rec 1 — Keep `design-significant: yes`. Correct as written, no change.
A tenth role file, a new `--check-crew` gate, a new binding relationship (one role's output makes others mandatory), and the first per-role budget — that is new interfaces plus a new module. Rejected: `no`. `no` means a pure fix; nothing here is a fix. Recording `yes` is not optional — the Architect gate reads the marker and never guesses.

rec 2 — Cite `docs/decisions/DECISION-007-agent-fleet.md` AND record a NEW `S135 addendum` in it. A citation alone is insufficient and would be a form-floor pass over a real deviation.
Both cited anchors exist (S133 addendum L862, S134 addendum L947), so the citation clears the gate. But the gate only checks the *form* of the citation, not whether the design obeys it — and S135 does not merely extend DECISION-007, it decides an open question the S134 addendum explicitly left undecided ("Deliberately NOT decided here… S135 must either fix the units or record, in writing, why", L986–997). rec 5 answers that question. A decision that resolves a locked record's open clause must be written back into the spine as an addendum, not left living only in a session prompt. Propose an `## S135 addendum — the tech-lead, and the threshold question answered` under DECISION-007, following the house pattern the S134 addendum itself names (L949–951).

rec 3 — Q1: handoff-only. The builder is right; adopt it. Do NOT mirror the decision into the session prompt.
The `tech-lead` handoff is provenance-verified and un-typed by the builder — that is the entire integrity claim. Rejected: mirroring the verdict into a builder-owned `## Crew` prompt section. Reason: it re-opens the "jurisdiction is self-granted" dodge S133 disclosed (a builder can edit its own file to fake the record), and it creates two sources of truth that will drift. The gate should read the handoff and nothing the builder can type. One caveat to record: handoff-only means a reader of the prompt alone cannot see the crew decision — that is an acceptable cost because `vajra next --check-crew NN` surfaces it on demand, but say so plainly rather than letting it look like an omission.

rec 4 — Q2: a required role's dispatch dying on a spend limit is a DISTINCT recorded outcome (`INCOMPLETE` / `deferred-budget` at close), NOT a bare block. The builder's instinct is right but incomplete — sharpen it.
The builder says "block stands + record INCOMPLETE, with `deferred-budget` as the pre-declared escape." Adopt that, with one precision: the block must stand *only* if no `deferred-budget` verdict covers that role. The clean path is — the `tech-lead` marks the role `required`; the dispatch dies mid-flight on the `$20` limit (as S134's judge did); the session cannot manufacture a handoff, so it re-runs the `tech-lead` decision to move that role to `deferred-budget` carrying the actual spend arithmetic that killed it (acceptance 2 already demands the arithmetic). That converts an un-actionable hard block into a recorded, checkable money fact — exactly the axis distinction the prompt draws. Rejected alternative A: a plain block with no recorded outcome — unhelpful when the account is genuinely out of budget, and it tempts the builder to upgrade an unjudged item to a pass. Rejected alternative B: auto-downgrade `required`→`deferred-budget` inside the gate on any missing handoff — that hands the builder a silent escape from every required role and guts the gate. The downgrade must be an explicit, arithmetic-carrying re-decision, never automatic.

rec 5 — Q3: give `tech-lead` NO migration threshold — pass `from_session: 0` (or equivalent "always enforce") at the call site. The builder is right, and this is the fix to the S134 brownfield hole, not a dodge — but it MUST be reasoned in writing in the S135 addendum (rec 2), because unexplained it looks identical to the hole.
A brand-new role has zero legacy prompts to exempt, so the threshold has no work to do; setting it to enforce-always means silence about the `tech-lead` blocks at every session, in every project, forever — which is precisely what the S134 addendum said the threshold *should* mean but doesn't ("measured in the wrong units"). This is buildable purely at the call site: `mandate_gate` already takes `from_session: u32` as a parameter (mod.rs L288, consulted only on rung 5/6), so the tech-lead call site passes `0` and no ladder code changes — the mechanism already supports "no exemption." Rejected alternative A: reuse `133` (the design-advisor threshold) — meaningless for a role that did not exist at S133 and would silently exempt S1–S134 of any brownfield adopter, importing the exact hole S134 named. Rejected alternative B: build a real units-fix now (git-birth-date or `.ai/` adoption marker) — over-engineering on n=1, and the addendum itself says n=1 does not earn a mechanism. The honest, minimal choice is: new role, no threshold, enforce from session one, and write down *why* so no future audit mistakes it for the S134 defect.

rec 6 — The crew gate CAN be built purely as a call site on `src/mandate`. I foresee NO forced edit to the shared ladder (`mandate_gate`/`parse_skip_marker`/`classify_marker_value`) — but I flag ONE real risk the builder must watch, and it is the session's falsification test.
The generic ladder already supports everything the base crew gate needs: `mandate_gate` is parameterized over `Role`, `from_session`, session, prompt and handoff (mod.rs L284–288), so "tech-lead required, no threshold, provenance-verified handoff or block" is a table entry plus a call, with `from_session: 0` (rec 5) and `advice::substantive_reason` reused verbatim (acceptance 2). The place I foresee pressure — flag it now — is acceptance criterion 4's second clause: "every role it marked `required` has its own real governed handoff." That is a NEW shape the S133 ladder never had: the existing ladder verifies *one* role's own handoff; the crew gate must, after verifying the `tech-lead` handoff, PARSE it for N required roles and then verify N *other* roles' handoffs. If the builder tries to express "verify the handoffs of the roles this handoff names" by widening `mandate_gate`'s signature or its return type, the genericity claim has failed and that is the headline finding (acceptance 5). The correct shape that keeps it a call site: run the generic `mandate_gate` once for `tech-lead`, then in crew-gate-specific code (new, not shared-ladder) read the verified handoff, extract the required roles, and call the *existing* per-role handoff verification (`dispatch`/`fleet::HandoffRead`, already imported at mod.rs L51–52) once per required role in a loop. No edit to `mandate_gate`, `parse_skip_marker`, or `classify_marker_value`. The builder must count and report shared-ladder lines vs call-site lines as a number (acceptance 5); my prediction on record: 0 lines added to the three shared-ladder functions. If that turns out false, the genericity was decoration and it becomes the session's lead finding.

One honesty note the author should carry into `## Advice`: I am the design-advisor being dispatched to satisfy S135's own S133 mandate (Plan step 1, acceptance 11), so I may not judge any `obeyed:` disposition against my own recommendations — a different role must grade those.

## Handoff Delta
- `+` new: first design-advisor handoff for this session (8002 bytes of findings)
- prior stage: the session prompt (Analyst WHAT) — no prior handoff to diff against
